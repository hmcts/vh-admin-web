apiVersion: v1
kind: Service
metadata:
  name: {{ include "vh-admin-web.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "vh-admin-web.name" . }}
    helm.sh/chart: {{ include "vh-admin-web.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "vh-admin-web.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
