import { MultipleIdpInterceptor } from './multiple-idp-interceptor';

describe('MultipleIdpInterceptor', () => {
    it('should create an instance', () => {
        expect(new MultipleIdpInterceptor()).toBeTruthy();
    });
});
