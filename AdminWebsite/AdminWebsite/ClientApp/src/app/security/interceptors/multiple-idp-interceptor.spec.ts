import { MultipleIdpInterceptorService } from './multiple-idp-interceptor';

describe('MultipleIdpInterceptor', () => {
    it('should create an instance', () => {
        expect(new MultipleIdpInterceptorService(null)).toBeTruthy();
    });
});
