import { UserActionDirective } from './user-action.directive';

describe('DisappearOnClickDirective', () => {
    it('should create an instance', () => {
        const directive = new UserActionDirective();
        expect(directive).toBeTruthy();
    });
});
